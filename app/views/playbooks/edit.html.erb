<div class="p-6 fade-in">
  <h1 class="text-lg font-semibold mb-4">Edit <%= @playbook.name %></h1>
  <%= form_with url: playbook_path(name: @playbook.name), method: :patch, local: true, class: "space-y-4" do |f| %>
    <div>
      <%= f.label :content, "YAML content", class: "block text-sm mb-1" %>
      <%= f.text_area :content, value: @playbook.content, rows: 20,
            data: { controller: "codemirror" },
            class: "w-full px-3 py-2 rounded text-sm font-mono",
            style: "background-color: var(--bg-primary); border: 1px solid var(--border);" %>
    </div>
    <div class="flex justify-end gap-2">
      <%= link_to "Cancel", playbooks_path,
            class: "px-3 py-1.5 rounded text-sm",
            style: "background-color: var(--bg-hover); color: var(--text-primary);" %>
      <%= f.submit "Save", class: "px-3 py-1.5 rounded text-sm",
            style: "background-color: var(--accent); color: white;" %>
    </div>
  <% end %>
</div>
